{
  "name": "lcars",
  "version": "1.0.0",
  "description": "A boardgame.io application",
  //"main": "index.html",
  "scripts": {
    "start": "parcel index.html --open",
    "build": "parcel build index.html --dist-dir build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "boardgame.io": "^0.50.2"
  },
  "devDependencies": {
    "parcel": "^2.0.0"
  },
  "browserslist": [
    "defaults and supports async-functions"
  ],
  "engines": {
    "node": ">=14.x"
  }
}
